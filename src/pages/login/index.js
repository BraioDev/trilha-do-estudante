import '../../style.css';
import BackButton from '../../components/backButton';

export default function Login() {
    return (
        <div>
            <p>Login</p>
            <BackButton />
        </div>
    );
}