import '../../../style.css';
import '../banco.css';
import CustomComponent from '../../../components/customJson/customJson';

export default function Heid() {
    return (
        <body style={{ backgroundColor: 'var(--cinza-escuro)'}}>
             <CustomComponent linguagem="Heid" />
        </body>
    );
}