import '../../../style.css';
import '../back.css';

export default function swagger() {
    return (
        <body style={{ backgroundColor: 'var(--preto-cinza)'}}>
            <div className="container">
                <div className="row">
                    <span>Swagger</span>
                </div>
            </div>
        </body>
    );
}