import '../../../style.css';
import '../back.css';

export default function spring() {
    return (
        <body style={{ backgroundColor: 'var(--preto-cinza)'}}>
            <div className="container">
                <div className="row">
                    <span>Spring</span>
                </div>
            </div>
        </body>
    );
}