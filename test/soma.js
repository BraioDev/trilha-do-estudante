/* const chai = require('chai');
const expect = chai.expect;
const soma = require('../api/math');

describe('Função de soma', () => {
    it('Deve somar dois numeros corretamente', () => {
        const resultado = soma(2, 3);
        expect(resultado).to.equal(5);
    });

    it('Deve somar dois numeros negativos corretamente', () => {
        const resultado = soma(-2, -3);
        expect(resultado).to.equal(-5);
    });

    it('Deve somar um numero negativo e um positivo corretamente', () => {
        const resultado = soma(2, -3);
        expect(resultado).to.equal(-1);
    });
}); */